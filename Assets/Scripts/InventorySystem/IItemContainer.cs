using System;

/*
public interface IItemContainer
{
	int InventorySize { get; }
	InventoryItem[] InventoryItems { get; }

	event Action<int> OnInventorySizeChanged;
	event Action<int, InventoryItem> OnInventoryItemChanged;

	void ModifyInventorySize(int sizeAdjustment);
	void AddNewItem(InventoryItem item, bool tryStack = true);
	void SwapItemsInSlots(int slotOne, int slotTwo);
	void SplitItem(int slot);
	void DropItem(int slot, bool dropStack);
	void RemoveItemsFromSlot(int slot, int amount);
	void BuyItemInSlot(InventoryHandler inventory, InventoryHandler otherInventory, int slot, bool buyingStack);
	void SellItemInSlot(InventoryHandler inventory, InventoryHandler otherInventory, int slot, bool sellingStack);
	void ResetContainer();

	bool InventoryFull();
	public bool SlotExists(int slot);
	public bool ItemExists(InventoryItem item);
	public bool ItemDefinitionMatches(InventoryItem itemOne, InventoryItem itemTwo);
}
*/
